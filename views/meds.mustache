<html>
    <head>
         <link rel="stylesheet" href="meds-style.css">
    </head>
    <body>
   
        <h1>This is the page that displays Meds<h1>

            <table>
            {{#rows}}
                <tr>
                    <td>{{id}}</td>
                    <td>{{name}}</td>
                    <td>{{count}}</td>
            
                    <td>
                        <form action="/meds/delete/{{id}}" method="post">
                            <button type="submit">delete</button>
                        </form>
                    </td>

                    <td>
                        <a href="/meds/edit/{{id}}">edit</a>
                    </td>
                </tr>
            {{/rows}}
            </table>
    
    </body>
</html>